# secrets
.env

# python
__pycache__/
*.pyc
*.pyo

# saved CRM profiles contain real customer PII — never commit
samples/*.html
profile_*.html
nbl_login_page.html
nbl_dashboard.html
nbl_search.html
nbl_profile_*.html

# local logs / debug output
*.log
acc.html

# auto-poller review queue — contains customer PII (mobile, email, name)
review_queue.jsonl

# auto-poller dedup log — transient state, regenerated at runtime
processed.jsonl

# Claude Code local settings (machine-specific)
.claude/

# editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
